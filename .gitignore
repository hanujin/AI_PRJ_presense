# Exclude results folder as requested
results/
checkpoints/
archive/
*.npy
*.pt

# Frontend build artifacts (the source itself must be versioned)
node_modules/
.next/
.env*

# Standard Python ignores
__pycache__/
.venv/
venv/
...
*$py.class

# OS junk
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
design.md
proposal.docx
명령어.md

# Frontend build artifacts
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/tsconfig.tsbuildinfo

# Local server output and development logs
server/*.log
*.log

# Model weights are deployed to the inference host separately, never committed.
models/
*.ckpt
*.pth
*.onnx
*.safetensors
*.h5

# Progress Report PDF — contains personal info (names, emails, phone numbers),
# and is no longer used by the app. Do not publish.
frontend/public/samples/

.env
.env.example
.env.local
# A non-secret template may be committed; real .env files remain ignored.
!server/.env.example

.obsidian/
.document/
